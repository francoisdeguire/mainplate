# mainplate

A React component API/library for building circular watchfaces.

> Status: pre-design. Nothing implemented yet — the API and architecture are still being planned.

## Name

In horology, the *mainplate* is the base plate every other component of a movement mounts onto. Same idea here: the substrate you build a watchface on top of.
